<Window x:Class="ModbusRTUScanner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ModbusRTUScanner"
        Style="{StaticResource CustomWindowStyle}"
        DataContext="{DynamicResource MainWindowViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>

    </Window.Resources>
    <Grid Grid.Row="1">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="MODBUS RTU SCANNER"
                 HorizontalAlignment="Left" 
                 Margin="10 -35 0 0"
                 FontSize="30"                    
                 FontFamily="{StaticResource MetronautsSuperItal}"                   
             />
        <StackPanel Grid.Row="1">
            <Grid>
                <GroupBox Header="COM Порт">
                    <ListBox ItemsSource="{Binding ComPortsList}"
                        SelectedItem="{Binding SelectedComPort}"
                        />
                </GroupBox>
                <GroupBox>
                    <GroupBox.Header>
                        <TextBlock Text="Параметры подключения" Style="{StaticResource  HeaderTextBlock}"/>
                    </GroupBox.Header>
                    <UniformGrid Rows="5" Columns="2">
                        <TextBlock Text="Speed: "/>
                        <TextBlock Text="{Binding PortSettings.BaudRate}" />
                        <TextBlock Text="DataBits: "/>
                        <TextBlock Text="{Binding PortSettings.DataBits}" />
                        <TextBlock Text="Parity: "/>
                        <TextBlock Text="{Binding PortSettings.Parity}" />
                        <TextBlock Text="StopBits: "/>
                        <TextBlock Text="{Binding PortSettings.StopBits}" />
                        <TextBlock Text="Address: "/>
                        <TextBlock Text="{Binding Address}" />
                    </UniformGrid>
                </GroupBox>
            </Grid>
            <Button Style="{StaticResource NightButtonStyle}"  
                    Width="101"
                    Command="{Binding SwitchThemeCommand}"
                    BorderThickness="0">
                <Button.Content>
                    <Image>
                        <Image.Style>
                            <Style TargetType="Image">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding FlagsManager.IsNightModeOn}" Value="True">
                                        <Setter Property="Source" Value="/Resources/Images/Night.png"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding FlagsManager.IsNightModeOn}" Value="False">
                                        <Setter Property="Source" Value="/Resources/Images/Day.png"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Image.Style>
                    </Image>
                </Button.Content>               
            </Button>
        </StackPanel>
    </Grid>
</Window>
